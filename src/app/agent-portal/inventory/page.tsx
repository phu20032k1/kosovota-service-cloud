"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { PortalHeader } from "@/components/ui/PortalHeader";
import { MetricCard } from "@/components/ui/MetricCard";
import { LoadingState } from "@/components/ui/LoadingState";
import { Notice } from "@/components/ui/Notice";
import { Icon } from "@/components/ui/Icon";

type Data={warehouses:{id:string;code:string;name:string;balances:{id:string;quantity:number;reserved:number;item:{sku:string;name:string;unit:string;minStock:number}}[]}[];movements:{id:string;movementCode:string;type:string;quantity:number;createdAt:string;item:{sku:string;name:string};fromWarehouse?:{name:string}|null;toWarehouse?:{name:string}|null}[];totals:{quantity:number;reserved:number;value:number;lowStock:number}};
const date=(v:string)=>new Intl.DateTimeFormat("vi-VN",{dateStyle:"short",timeStyle:"short"}).format(new Date(v));
export default function DealerInventoryPage(){const[data,setData]=useState<Data|null>(null);const[error,setError]=useState("");useEffect(()=>{fetch("/api/inventory",{cache:"no-store"}).then(r=>r.json().then(j=>({r,j}))).then(({r,j})=>{if(!r.ok||!j.success)throw new Error(j.message);setData(j.data)}).catch(e=>setError(e.message));},[]);const balances=data?.warehouses.flatMap(w=>w.balances.map(b=>({...b,warehouse:w})))||[];return <main className="min-h-screen"><PortalHeader title="Kho vật tư đại lý" subtitle="Theo dõi tồn thực tế và lịch sử nhận/xuất vật tư" homeHref="/agent-portal" homeLabel="Lệnh dịch vụ" onLogout={()=>fetch("/api/auth/logout",{method:"POST"}).then(()=>location.assign("/login"))}><Link href="/agent-portal" className="btn-secondary"><Icon name="wrench" size={16}/>Lệnh dịch vụ</Link><Link href="/agent-portal/payments" className="btn-secondary"><Icon name="wallet" size={16}/>Đối soát</Link></PortalHeader><div className="mx-auto max-w-7xl space-y-6 p-4 sm:p-6">{error&&<Notice kind="error">{error}</Notice>}{!data?<LoadingState label="Đang tải kho đại lý..."/>:<><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Tổng tồn" value={data.totals.quantity} icon="package" tone="emerald"/><MetricCard label="Đang giữ chỗ" value={data.totals.reserved} icon="lock" tone="blue"/><MetricCard label="Cảnh báo thấp" value={data.totals.lowStock} icon="alert" tone="rose"/><MetricCard label="Kho đang quản lý" value={data.warehouses.length} icon="store" tone="violet"/></section><section className="surface-card"><div className="data-toolbar"><div><h2 className="page-section-title">Tồn vật tư</h2><p className="page-section-subtitle">Vui lòng liên hệ điều phối khi cần bổ sung hàng</p></div></div><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead><tr>{["Kho","Mã","Vật tư","Tồn","Giữ chỗ","Khả dụng","Đơn vị"].map(h=><th key={h} className="p-3 text-left">{h}</th>)}</tr></thead><tbody>{balances.map(b=><tr key={b.id}><td className="p-3 font-bold">{b.warehouse.name}</td><td className="p-3 font-black">{b.item.sku}</td><td className="p-3">{b.item.name}</td><td className="p-3 font-black">{b.quantity}</td><td className="p-3">{b.reserved}</td><td className="p-3 font-black text-emerald-700">{b.quantity-b.reserved}</td><td className="p-3">{b.item.unit}</td></tr>)}</tbody></table></div></section><section className="surface-card"><div className="data-toolbar"><h2 className="page-section-title">Giao dịch gần đây</h2></div><div className="divide-y">{data.movements.slice(0,20).map(m=><div key={m.id} className="flex flex-wrap items-center justify-between gap-3 p-4"><div><p className="font-black">{m.movementCode} · {m.item.name}</p><p className="text-xs text-slate-500">{m.fromWarehouse?.name||"Nguồn ngoài"} → {m.toWarehouse?.name||"Đã xuất sử dụng"}</p></div><div className="text-right"><strong>{m.type} · {m.quantity}</strong><p className="text-xs text-slate-500">{date(m.createdAt)}</p></div></div>)}</div></section></>}</div></main>}
