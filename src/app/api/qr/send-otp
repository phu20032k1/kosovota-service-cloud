export async function POST(req: Request) {
  const { phone } = await req.json();

  // 1. tạo OTP
  const otp = Math.floor(100000 + Math.random() * 900000);

  // 2. lưu OTP (tạm DB hoặc memory)
  console.log("OTP:", otp);

  // 3. gọi SMS API (chưa gắn thật ở bước này)
  // sẽ gắn eSMS sau

  return Response.json({
    success: true,
    message: "OTP sent",
  });
}